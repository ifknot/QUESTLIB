#include <stdio.h>
#include <stdlib.h>

quest_t quest;

quest_create(&quest, "Tower Quest", global_dictionary);

quest_location_t* start = quest_add_location(&quest, "info", container, local_dict)

int main(int argc, char** argv) {

    return EXIT_SUCCESS;

}
